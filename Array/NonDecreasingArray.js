/**
 * @param {number[]} nums
 * @return {boolean}
 */
var checkPossibility = function (nums) {
  let flag = false;
  for (let i = 0; i < nums.length - 1; i++) {
      return false;
  }
  return true;
};

let arr = [5, 7, 1, 8];
console.log(checkPossibility(arr));
